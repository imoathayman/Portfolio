# Portfolio — Build Brief

مرجع التنفيذ. الـ mockup النهائي في `hero-mockup.html` — هو المرجع البصري والسلوكي.

---

## 1. Stack

- Vue 3 (Composition API, `<script setup>`)
- Vite
- Tailwind CSS v4 (`@import "tailwindcss"` — بدون `tailwind.config.js` منفصل)
- vue-router — كل قسم route مستقل
- vue-i18n — عربي/إنجليزي
- بدون Pinia: الحالة قليلة (اللغة + القسم الحالي)، `router` + composable واحد بيكفوا

---

## 2. القواعد غير القابلة للتفاوض

1. **مافي `left` / `right` في أي CSS.** فقط `inset-inline-start`, `inset-inline-end`, `margin-inline`, `padding-inline`. الموقع بينقلب بتغيير `dir` على `<html>` فقط.
2. **مافي scroll للصفحة.** `body { overflow: hidden }`، وكل قسم `100dvh` (مش `100vh`).
   **استثناء موبايل (قرار ٢٠٢٦-٠٨-١٢، وُسّع ٢٠٢٦-٠٨-١٩):** على الموبايل، المحتوى المكدّس بيتجاوز `100dvh` — وبما إنه مافي scroll، الزيادة بتنقص نهائياً وما بيقدر المستخدم يوصلها. القرار: **كل قسم محتواه ممكن يتجاوز الشاشة بياخذ scroll داخلي على الموبايل فقط** (`overflow-y:auto` + الـ wrap بيرجع `position:static`). مطبّق على: **About, Skills, Experience, Contact**. (Hero و Projects بيضلوا داخل الشاشة، فما بياخدوا scroll.)
   القاعدة الأصلية (مافي scroll) لسه سارية بالكامل على الديسكتوب.
   ⚠️ لما الـ wrap يصير `position:static`، أي `inset-inline-*` عليه بيصير **كود ميت** — الهوامش الجانبية لازم تجي من `padding-inline`. (هاد صار فعلاً بـ Experience وخلّى النص لازق بحافتي الشاشة.)
   الأرقام كلها من tokens بـ `style.css`: `--gutter-mobile`, `--pad-block-mobile`, `--dock-clearance`, `--tap-min`.
3. **مافي `localStorage` للحالة** غير تفضيل اللغة.
4. **الـ delay في transitions بينحط على حالة الدخول فقط**، مش على العنصر الأساسي — وإلا الخروج بيصير كسول (هاي كانت bug فعلي وانحلت).
5. **الأقسام المخفية لازم تكون `inert` + `aria-hidden`** — مش بس `opacity: 0`.
6. **`prefers-reduced-motion`**: يلغي الدوران والآلة الكاتبة، وينتقل بـ crossfade 200ms.

---

## 3. Design tokens

```css
:root{
  /* colour */
  --color-primary:        #5FA8D3;  /* accent فقط: أشكال، أيقونات، حدود */
  --color-primary-deep:   #2C6E9E;  /* fill الأزرار — نص أبيض فوقه */
  --color-soft:           #B4D3E8;  /* حدود، خطوط فاصلة، حالة غير نشطة */
  --color-ink:            #2B2B28;  /* نص أساسي */
  --color-ink-2:          #726E66;  /* نص ثانوي */
  --color-bg:             #FDFCF9;
  --color-bg-alt:         #F6F1E6;

  /* motion */
  --dur-fast:   180ms;
  --dur:        420ms;
  --dur-spin:  1100ms;
  --ease:       cubic-bezier(.65,0,.35,1);
  --ease-spin:  cubic-bezier(.72,0,.22,1);
}
```

**تحذير تباين (محسوب، لا يتغيّر):**

| تركيبة | نسبة | حكم |
|---|---|---|
| أبيض على `#5FA8D3` | ~2.4:1 | ❌ ممنوع |
| `#2B2B28` على `#5FA8D3` | ~6.0:1 | ✅ |
| أبيض على `#2C6E9E` | ~5.5:1 | ✅ |
| `#5FA8D3` كنص على `#FDFCF9` | ~2.4:1 | ❌ ممنوع |

القاعدة: `--color-primary` **ما بيحمل نص أبيض أبداً**. أي زر معبّى بياخد `--color-primary-deep`.

**Typography:** Cairo فقط. 700 عناوين · 600 عناوين فرعية · 400 نص · 300 تسميات صغيرة (نادر). حد أقصى وزنين بنفس الشاشة. العربي بيحتاج `line-height` أعلى (~1.34 مقابل 1.16 للاتيني) وحجم أصغر ~8% للتوازن البصري.

---

## 4. البنية المعمارية للتنقل

**القرص الدوار** — أهم قرار في المشروع:

- ٦ أقسام مركّبة على محيط دائرة، كل واحد على بُعد `--step: 30deg`
- نصف القطر `--R` = قطر الشاشة القُطري × 2.15، بيتحسب عند `resize`
- القرص بيلف بـ `rotate(calc(var(--cur) * var(--step) * -1))`
- **كل قسم بيلف عكس القرص بنفس المقدار** ليضل مستوي — زي كبائن العجلة الدوارة
- مدة 1100ms بـ `--ease-spin`
- الـ chrome (شعار، مبدّل لغة، dock، عدّاد) **خارج القرص** — ثابت لا يلف

المقبض القابل للضبط: الرقم `2.15`. أصغر = قوس أوضح، أكبر = أقرب لانزلاق مستقيم.

**Routing:** كل قسم route (`/`, `/about`, `/skills`, `/projects`, `/experience`, `/contact`) — عشان deep linking وزر الرجوع. الأقسام كلها mounted دائماً (مش `v-if`)؛ الـ route بيغيّر `--cur` فقط.

---

## 5. الأقسام

### 5.1 Hero
- حقل أزرق عضوي (`border-radius` بأربع قيم مختلفة) خارج من زاوية النهاية + طبقة `--color-soft` بشفافية 55% + حلقة رفيعة
- صورة شخصية PNG مفرّغة، **راس وكتف طالعين فوق حافة الأزرق** (مقصود — مش مقصوصة داخله)
- **Hook عشوائي كل تحميل**: مصفوفة من ٥ عناوين لكل لغة، اختيار عشوائي مع استثناء آخر واحد ظهر (`sessionStorage`)
- **آلة كاتبة للمسميات**: الإنجليزي حرف بحرف (55ms)، **العربي كلمة بكلمة (150ms)** — العربي خط متصل وشكل الحرف بيتغير حسب موقعه، فالكتابة حرف بحرف بتخلي الكلمة ترجّ
- نسخة ثابتة من المسمّى في `.sr-only` للقارئات، والمتحرك `aria-hidden`

### 5.2 About
عمودين: عنوان كبير + فقرة | قائمة ٤ نقاط بخط جانبي (من فكرة لنظام · أمان من الأساس · واجهة سهلة · كود نظيف)

### 5.3 Skills
٤ طبقات على عمود فقري: البيانات ← الخادم ← الواجهة ← التسليم. كل طبقة: اسم + سطر مسؤولية + شيبس أدوات.
**مافي نسب مئوية** — رقم "85%" ادعاء دقة كاذب وأول سؤال تقني بيفضحه.

### 5.4 Projects
- صف أفقي، العنصر النشط `scale(1.24)` + `--color-primary-deep`، الباقي `--color-soft`
- شريط تحت الصف بيتحدّث فوراً باسم/وصف النشط + زر "شوف التفاصيل"
- الضغط → **FLIP**: قياس `getBoundingClientRect()` للكرت، وضع اللوحة بنفس المكان/الحجم، ثم تحريرها لنصف الشاشة (620ms)
- الخروج مرآة الدخول (560ms) — نفس الحساب بالعكس
- زر خروج فقاعة 46px فوق الزاوية، بيظهر بعد 420ms
- عند الفتح: `body.detail-open` → الـ chrome كله بيختفي fade 340ms

### 5.5 Experience
مساران حول محور أفقي: **فوق = عمل** (٥) · **تحت = تعلّم** (٦). عقد معبّاية للعمل، مفرغة للتعلّم. فلاتر (الكل/عمل/تعلّم) بتخفّت المسار الآخر لـ `opacity: .22` بدل ما تخفيه.

### 5.6 Contact
عمودين: روابط مباشرة (إيميل، لينكدإن، جيت-هاب) | فورم. الحقل الأزرق بيرجع معكوساً من الزاوية المقابلة — الموقع بيفتح وبيسكّر بنفس اللغة البصرية.

---

## 6. الـ dock

٦ علامات SVG (18×18، `stroke-width: 1.6`) بتعبّر عن كل قسم بدون كلمات:

| قسم | علامة |
|---|---|
| البداية | دائرة بنقطة مركزية |
| نبذة | ٣ سطور متدرّجة |
| المهارات | ٣ أعمدة بارتفاعات مختلفة |
| المشاريع | شبكة 2×2 |
| الخبرة | خط زمني بعُقد |
| تواصل | سهم خارج |

الاسم tooltip عند hover/focus فقط. **مافي أي اسم قسم مطبوع على الشاشة.**

---

## 7. متروك للتنفيذ (لسه ناقص)

- [ ] **لقطات المشاريع** — أهم نقطة، حالياً wireframes وهمية
- [ ] **SVG للشعار** — لسه PNG، والمربع الأزرق في الـ mockup مؤقت
- [ ] نسخة favicon أحادية اللون
- [ ] تحويل الصورة الشخصية لـ WebP (١.٩MB حالياً)
- [ ] تاريخ دعوة الزفاف في Experience
- [ ] روابط DENT و Burgelo (حالياً `#`)
- [ ] باك اند الفورم (Formspree أو ما شابه)
- [ ] مراجعة النصوص العربية
- [ ] **النسخة الموبايل** — القواعد موجودة بس ما تم اختبارها فعلياً؛ القرص الدوار تحديداً بده مراجعة على شاشة ضيقة

## 8. SEO

الـ `<h1>` بيتغير كل زيارة — مقايضة مقصودة. للتعويض: `<title>` و`meta description` **ثابتان تماماً**، وكل الـ hooks بتحمل نفس المعنى.
